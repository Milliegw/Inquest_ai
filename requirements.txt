# Inquest AI Document Analysis Pipeline - Dependencies
# Install with: pip install -r requirements.txt
#
# SECURITY NOTE: Versions are pinned to prevent pulling vulnerable packages.
# Run `pip-audit` periodically to check for CVEs and update as needed.

# Core LlamaIndex (pinned for security)
llama-index-core>=0.12.4,<0.13
llama-index-embeddings-ollama>=0.3.0

# Vector Store - Milvus Lite (embedded, no Docker required)
pymilvus[milvus_lite]>=2.5.10,<3
llama-index-vector-stores-milvus>=0.2.0

# LLM Providers
llama-index-llms-ollama>=0.6.0,<0.7
llama-index-llms-groq>=0.2.0

# Reranking for better retrieval precision
sentence-transformers==3.3.1

# Environment and configuration
python-dotenv==1.0.1

# PDF processing
pdfplumber==0.11.8

# Ollama client (for local embeddings)
ollama>=0.5.1

# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# Async support
nest-asyncio>=1.6.0

# Security scanning (recommended for development)
pip-audit>=2.7.0

# Optional: OpenAI for querydoc.py
# openai==1.58.1
